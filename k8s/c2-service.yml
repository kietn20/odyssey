apiVersion: v1
kind: Service
metadata:
  name: c2-service
spec:
  type: NodePort
  selector:
    app: c2
  ports:
    - name: http
      protocol: TCP
      port: 8081
      targetPort: 8081
      nodePort: 30081